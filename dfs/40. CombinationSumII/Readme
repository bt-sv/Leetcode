这道题是很明显的回溯题(DFS的一种)，即创造现场，还原现场
1. 首先将数组排序，这样做为了更容易的判断sum
2. 从第一个位置开始便利数组，每次便利的时候把元素放到list里面，同时更新sum的值(创造现场)，当sum = target时则把整个list放入结果中，然后把这个元素从list中取出(还原现场)，从而进行下一次便利
注意的是: 当前元素的值和前一个元素的值相等，而且之前的元素已经结束被访问(结束还原现场)的时候，则跳过这次判断。
举个例子:
       0, 1, 2, 3, 4, 5
arr = [1, 1, 1, 3, 4, 5]
如果index = 0的数字1已经遍历完(已经还原现场)，那么index = 1的数字1就无需遍历。



